<script type="text/javascript">
    var searchDialog;
    var module = "SortBatch";
    var searchKey = false;
    var addKey = false;
    var editKey = false;
    var deleteKey = false;
    var parentComNameKey = false;

    function searchClick() {
        $("input[type=text]").attr("value", "");
        $('#searchStatus').combo({ editable: false });
        $('#searchStatus').combo('setValue', "");
        $('#searchOrderDate').datebox({ editable: false });
        $('#searchOrderDate').datebox('setValue', "");
        if (searchDialog == null) {
            searchDialog = $('<div></div>').dialog({
                title: '查询',
                width: 280,
                height: 200,
                closed: true,
                cache: false,
                modal: true,
                href: '/SortBatch/SearchPage/',
                buttons: [{
                    text: '查询',
                    iconCls: 'icon-ok',
                    handler: function () {
                        GetQueryParams();
                    }
                }, {
                    text: '取消',
                    iconCls: 'icon-cancel',
                    handler: function () {
                        searchDialog.dialog('close');
                    }
                }]
            });
        }
        searchKey = true;
        searchDialog.dialog('open');
    };
    function GetQueryParams() {
        var queryParams = $('#details').datagrid('options').queryParams;
        queryParams.Status = $('#searchStatus').combo('getValue');
        queryParams.BatchName = $('#searchBatchName').val();
        queryParams.OrderDate = $('#searchOrderDate').datebox('getValue');
        queryParams.BatchNo = $('#searchBatchNo').val();
        $('#details').datagrid('options').queryParams = queryParams;
        $("#details").datagrid('load');
        searchDialog.dialog('close');
    }
</script>
<script type="text/javascript" src="../../Scripts/mykeyboardshortcuts.js"></script>