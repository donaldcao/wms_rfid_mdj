<script type="text/javascript">
    function editClick() {
        var row = $('#details').datagrid('getSelected');
        if (row) {
        var s;
            editInit(row);
            if (row.SortingLineCode == '01') {
                $('#sortNO').combo('setValue', '01').combo('setText', row.SortingLineCode);
            }
            else {
                $('#sortNO').combo('setValue', '02').combo('setText', row.SortingLineCode);
            }
            url = '/SortOrderDispatch/Edit/?id=' + row.ID;
        }
        else {
            $.messager.alert('错误', '没有选择行！');
        }
    }
function editInit(row) {
    $('#dlg-edit').dialog('open').dialog('setTitle', '编辑');
    //文本赋值&下拉框赋值
    $('#editFm').form('load', row);
    //恢复Form中的文本的有效验证
    $('#editFm .easyui-validatebox').validatebox({ required: true });
}
</script>
   