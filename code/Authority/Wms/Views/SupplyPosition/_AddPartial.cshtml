<script type="text/javascript">
    var url;
    var type;

    $(function () {
        addInit();
    });

    //初始化
    function addInit() {
        $('#productCode2').attr('readonly', 'true');
        $('#productName2').attr('readonly', 'true');
        $('#isActive2').combo({ editable: false, width: 130, panelHeight: 'auto' });
    }

    //添加
    function addClick() {
        $('#dlg-add').dialog('open').dialog('setTitle', '新增');
        $('#addFm').form('clear');
        //恢复Form中的文本的有效验证
        $('#addFm .easyui-validatebox').validatebox({ required: true }); 
        //恢复Form中禁用的文本
        $('form input').removeAttr("disabled");
        $('form textarea').removeAttr("disabled");
        url = '/SupplyPosition/Add/';
    }

    //保存
    function save() {
        $('#addFm').form('submit', {
            url: url,
            onSubmit: function () {
                return $(this).form('validate');
            },
            success: function (result) {
                var result = eval('(' + result + ')');
                if (result.success) {
                    $.messager.alert(g_MsgBoxTitle, result.msg, "info");
                    $('#dlg-add').dialog('close');
                    $('#details').datagrid('reload');
                } else {
                    $.messager.alert(g_MsgBoxTitle, result.msg + '<br />' + result.data, "error");
                    $('#details').datagrid('reload');
                }
            }
        });
    }
</script>
<style type="text/css">
    #dlg-add{ width: 500px; height: 260px; padding: 0px 0px;}
    .div1{ padding: 10px 10px; background: #fff; border: 1px solid #ccc;}
    .addtable { width: 480px; padding: 0px 0px; }
</style>
<div id="dlg-add" class="easyui-dialog" modal="true" closed="true" buttons="#dlg-buttons">
    <div class="easyui-layout" fit="true">
        <div region="center" border="false" class="div1">
            <form id="addFm" method="post" novalidate>
            <table class="addtable">
                <tr>
                    <td><input name="ID" style="display: none" /></td>
                </tr>
                <tr>
                    <td>
                        任务类型
                    </td>
                    <td style="padding-right: 30px;">
                        <select id="positionType2" name="PositionType" class="easyui-combobox">
                            <option value=""></option>
                            <option value="01">正常拆盘位置</option>
                            <option value="02">混合拆盘位置</option>
                        </select>
                    </td>
                    <td>
                        状态
                    </td>
                    <td>
                        <select id="isActive2" name="IsActive" class="easyui-combobox">
                            <option value=""></option>
                            <option value="0">可用</option>
                            <option value="1">禁用</option>
                        </select>
                    </td>
                </tr>
                <tr>
                    <td>
                        商品编码
                    </td>
                    <td>
                        <input id="productCode2" name="ProductCode" class="easyui-validatebox" required="true"/>
                        <a href="#" onclick="productQuery()" class="easyui-linkbutton" iconcls="icon-search"
                            plain="true"></a>
                    </td>
                    <td>
                        商品名称
                    </td>
                    <td>
                        <input id="productName2" name="ProductName" />
                    </td>
                </tr>
            </table>
            </form>
        </div>
    </div>
</div>
<div id="dlg-buttons">
    <a href="#" class="easyui-linkbutton" iconcls="icon-ok" onclick="save()">确定</a>
    <a href="#" class="easyui-linkbutton" iconcls="icon-cancel" onclick="javascript:$('#dlg-add').dialog('close')">
        取消</a>
</div>
